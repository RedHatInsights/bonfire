# Bonfire deployment configuration

# Optional: define remote application config file origin
# appsFile:
#   host: github
#   repo: some-org/some-repo
#   path: config/app_templates.yaml

# Define any app config locally. An app defined here with <name> will override config for app
# <name> in any previously fetched app config.
# apps:
# - name: my_custom_app
#   components:
#     - name: my_app_component
#       host: github
#       repo: some-org/some-repo
#       path: deploy/template.yaml
